# PLANS — Instagram Automation (POWERELIX)

## 🎯 Стратегический этап (перед публикацией) — в работе с 2026-06-08
Решено сначала проработать стратегию, потом публиковать. Порядок:
- ✅ **A. Фундамент бренда** — позиционирование, миссия, ценности, тон, слоган «Здоровье. Энергия. Каждый день.» (см. `docs/brand_foundation.md`). Визуальная система выбрана: **1+3** (светлый минимализм лента + тёплый лайфстайл Reels, тёмный — спорт) → `docs/visual_guide.md`.
- ✅ **B. Анализ конкурентов** — qeep/pwr (РФ) + ritual/ghost (US) через Apify → `docs/competitor_analysis_2026-06.md`.
- ✅ **C. Шапка профиля** — слоган + bio + имя (категории) + Taplink; установлена вручную.
- **D. Инструменты генерации** — подход **ГИБРИД**. Картинки: **gpt-image через ProxyAPI** (тем же ключом, что Claude — работает, отдельный сервис пока не нужен). Видео: микс (съёмка + AI, позже). Ключевой приём: реальная банка в AI-сцену; для финала — фон-генерация + вырез банки (чтобы этикетка не искажалась). Реальные фото банок: `~/Downloads/Архив/`.
  - Генератор шаблона поста на Pillow — `ig_automation/post_template.py` (`render_post`): кремовый холст, логотип, Inter ExtraBold заголовок, акцент по цвету продукта, фирменные `{ }`, вырез реальной банки с белого фона (чёткая этикетка). Шрифты Inter/Montserrat уже в системе.
  - **Брендовые надписи-водяные знаки** — `ig_automation/brand_overlay.py` (`render(style,...)`), СВОЯ бренд-ДНК (не копия PWR). Три стиля на пробу: `stamp` (вордмарк+знак, заголовок Montserrat Black, акцент-скобка, слоган-стрип), `chips` (фирменные `{ }` как инфо-чипы вокруг банки), `xmark` (гигантский фирменный знак полупрозрачной подложкой). Фирменный знак — **настоящий** `assets/brand/mark.png` (изолированный, прислал пользователь; белый фон → прозрачность на лету в `brand_mark()`); кодовая версия осталась fallback'ом. Раскладка продукт→банка→акцент(hex)→короткий заголовок/подзаголовок/капсулы — `data/product_assets.json` (все 11 опознаны по контактному листу); `render_product(id, style)` тянет банку из `~/Downloads/Архив`, акцент и реальные капсулы/дни (чипы больше не хардкод). Батч: `output/lineup/` (11×3) + листы сравнения по стилю. TODO: **выбрать финальный стиль** (stamp/chips/xmark); банки-исходники пока в Downloads (не в репо).
  - **Hero-оверлей для сцен/каруселей** — `render_hero(style, title, bg_path, ...)`: наш бренд поверх ЛЮБОЙ фотосцены. Первая версия копировала каркас PWR (верхний бар + коллаб-слэм) → **пересобрано на своих каркасах**: `block` (чистый блок + скобка), `band` (градиент-лента lime→бирюза со слоганом), `anchor` (знак-якорь). bg_path=None → тёмная заглушка (реальные сцены — после генератора, фаза D). TODO: выбрать каркас; в anchor сдвинуть знак с текста.
  - **Генерация сцен (Replicate) — фаза 1 готова.** `ig_automation/scenes.py` (`generate_scene(prompt, ratio, hq, model)`) + CLI `generate_scene.py`: промпт → сцена 4:5/9:16/1:1 → `output/scenes/`. Паттерн из wb-design (`call_replicate` + sanitize, `Prefer: wait`). Дефолт-модель `flux-dev` (env `IMAGE_MODEL`), `--hq` → `flux-1.1-pro-ultra`. Текст рисуем оверлеем, в промпт — «no text/no watermark». Нужно: вставить `REPLICATE_API_TOKEN` в `.env` (из wb-design). Верифицировано вживую (танц-сцена 1080×1350). План: `plans/scene-generation-2026-06-09.md`.
  - **Мульти-модель + Grok (2026-06-10).** `scenes.py` теперь маршрутизирует по модели: Replicate (flux/flux-ultra), `openai/gpt-image-1` (требует `OPENAI_API_KEY` как input — берём из соседнего `wb-design/.env`, маппинг 4:5→2:3), `google/nano-banana` (короткий негатив — длинный `_NO_TEXT` детерминированно ловит safety E005), и **Grok xAI** напрямую (`grok-imagine-image-quality`, `https://api.x.ai/v1/images/generations`, `_call_xai_image`, 4:5→3:4, `XAI_API_KEY`). Сравнение 3 моделей на «сочном» промпте → **победитель Grok** (самый красочный изумруд, уровень WB-карточек); nano-banana 2-е, gpt-image тёмное. Записано в память `reference_grok_image_gen`.
  - **AI-модель бренда + reference-пак (готово).** Постоянное «лицо» POWERELIX — **вариант D** (~24, медные волнистые волосы, веснушки, стройная; ультра-реалистичная кожа: поры/пушок/без ретуши). Reference-пак в `assets/brand/`: `ai_model.png` (якорь), `model_sheet.png` (ракурсы лица+фигуры), `expression_sheet.png` (эмоции) — чтобы девушка была ВЕЗДЕ одинаковой (лицо И телосложение). Консистентность через Grok **`images/edits`** (`_call_xai_edit`): референс из пака → лицо/фигура те же, сцена меняется промтом. Мультиреференс (`images[]` = лицо + `bottle_ref.jpg`) → держит НАСТОЯЩУЮ банку, этикетка «ХЛОРОФИЛЛ» сохраняется. data URL в `image.url`, хостинг не нужен. 100% пиксельный лок тела = только LoRA (через xAI API недоступно). Память: `project_powerelix_ai_model`.
  - **Карусель Пост №1 (готово, `build_post01.py`):** S1 — AI-модель уставшая (вечер, диван, `grok_s1_girl.png`); слайды 2–5/7 — кремовый фирменный однотон с текстом; **S6** — та же модель держит банку хлорофилла (`grok_s6_girl.png`, мультиреференс лицо+банка), сочный изумруд + всплеск + мята, текст-поверх-фото `hero_product_slide` + `_greendim`. ⚠️ Урок: «живая/сочная» = про hero-картинки (1, 6), а не заливать зелёным все слайды (версия «все 7 на зелёном» отбракована); средние слайды — чистый однотон. TODO: Reels-версия; Пост №2 (Инозитол) на той же модели.
  - **Фаза 2 готова (связка с оверлеем):** CLI `generate_scene.py` += `--overlay block|band|anchor`, `--title`, `--subtitle`, `--product <id>`. `render_hero(... product_id=)` врезает реальную банку в сцену (компактный инсет) + тянет акцент/заголовок/подпись из `product_assets`. Lifestyle-обложка (без продукта) — отлично; продуктовые карточки требуют **продуктовых сцен-подложек** (стол/подиум, без людей) — это в фазе 3. Дальше фаза 3: промпт-шаблоны по visual_guide (lifestyle/dark/light + продуктовые подложки) + 9:16 + batch.
- **E. Плейбуки** — визуал + цепляющие заголовки, копим в prompt-lab (по референсам/урокам от пользователя).
- **F. Публикация** — постинг/Reels/Stories по плану + расписание.

## 🏭 Сервис «Контент-завод» (FastAPI) — строится с 2026-06-10
- ✅ **Бэкапы завода** (2026-07-14): `/home/bidder/backup-cf.sh` — ежедневно 04:30
  (cron): sqlite `.backup` content_factory.db → gzip → `gdrive:content-factory-backups/db/`
  (ротация 14 дн) + инкрементальное зеркало `data/media`, `data/product_refs`
  и json-конфигов в `gdrive:content-factory-backups/data/`. Первый прогон:
  206 МБ медиа в Drive.
- ✅ **Пакет для ручной публикации** (2026-07-15): кнопка «📦 Пакет для
  ручной публикации» на карточке поста → ZIP: слайды 01..N + готовые
  подписи под каждую площадку (IG с хэштегами / TG без хэштегов /
  VK со ссылками) + README. GET `/post/{id}/manual-pack`.
- ✅ **Выбор площадок при публикации** (2026-07-15): на карточке поста
  чекбоксы IG/TG/VK у «Опубликовать сейчас» и «Запланировать» (по умолчанию
  все). `publisher.publish(post_id, platforms)`; для отложенных — колонка
  `posts.platforms` (CSV, ""=все), планировщик её уважает. Без IG пост
  помечается published сразу после кросс-постов на выбранные площадки.
- ✅ **Умный оверлей заголовка** (2026-07-15): `producer.smart_overlay` —
  Claude-vision смотрит на готовый слайд и выбирает угол для крупного
  заголовка, где он не перекроет банку/этикетку/лицо (top/bottom × left/right);
  чистой зоны нет → слайд без заголовка (только вордмарк POWERELIX + тень).
  Правые углы — выравнивание строк по правому краю, ширина блока ≤48% кадра,
  максимум 3 строки. Заменяет слепой render_cover снизу-слева.
- ✅ **Тумблеры «что в кадре» на раскадровке** (2026-07-22): две независимые
  галочки на `/storyboard/{id}` — 👤 человек и 🫙 наша банка (колонки
  `storyboards.include_model/include_product`, дефолт вкл). 4 комбинации:
  человек+банка (как было) / только банка / только человек / **ни того ни
  другого = чистый концепт** (капсула/абстракция, как Pinterest-референс).
  `producer._produce_slides` собирает референсы и промпт под тумблеры:
  нет человека → «в кадре нет людей вообще»; нет банки → «продукт как на
  референсе (капсула/концепт), без фирменной баночки» + `gen_product_image`
  пропускает vision-проверку этикетки (`check_label=bool(bottle)`), чтобы не
  гонять цепочку впустую. Роут `POST /storyboard/{id}/scene`.
- ✅ **Заголовок никогда на лице** (2026-07-21): угол выбирается ГЕОМЕТРИЕЙ,
  а не «на глаз» (vision путался и клал текст на лицо). `_overlay_boxes` →
  vision возвращает нормированные рамки ЛИЦА и БАНКИ; `_pick_spot` считает
  реальный прямоугольник заголовка (bh×tw) и проверяет пересечение. Два
  приоритета: **лицо пересекать нельзя НИКОГДА** (жёстко, запас 5% высоты),
  банку — избегаем (мягко), но если чистого угла нет, текст ляжет на корпус
  банки (меньшее зло). Совсем нет места мимо лица → 'none' (только вордмарк).
- ✅ **Нативный 9:16 без размытых полос + вордмарк-текст** (2026-07-23): (1) `gen_image_seedream` (дефолт цепочки постов) не передавал формат → Seedream отдавал своё соотношение, `_fit_ratio` добивал размытой подложкой (полосы сверху/снизу). Теперь `image_size=_native_size(aspect)` (проверено live: 864×1536 = ровно 9:16) → кадр заполняет формат нативно. Хелпер `_native_size(aspect, cap=1536)`. (2) Вордмарк 9:16: пробовал «пилюлю» (тёмная плашка+мятная точка) — не зашла; вернул простой белый текст POWERELIX (`_wordmark`), опущен ниже (y=0.075). `stamp_wordmark` ставит его на ВСЕ генерации (сравнение/инфографика/Reels), из промпта aicopy убран «нарисуй вордмарк».
- ✅ **Источники: забор «топ по просмотрам» vs «свежие»** (2026-07-23): Apify instagram-scraper отдаёт ленту новые→старые, по просмотрам не сортит. `account_reels(scan_limit)` — широкое окно (limit×3) → топ-limit по play_count. Две кнопки на источнике: 🔍 Свежие / 🔥 Топ по просмотрам. Потолок расхода Apify масштабируется с окном.
- ✅ **Сравнение «по мотивам» + нативный 9:16 (gpt-image-2 на fal)** (2026-07-23): режим `aicopy` — воспроизводит референс-инфографику узнаваемо (тот же главный визуал/сюжет/композиция), но своим брендингом (наши банки, ровно N штук, фирменная зелень, убран чужой лого) для анти-плагиата. **Формат 9:16 теперь нативный**: `gen_image_gpt` маппит соотношение в пресет fal (`9:16→portrait_16_9` 1024×1792, `16:9→landscape_16_9`, `1:1→square_hd` и т.д.), `_execute_aicopy` передаёт реальный `ratio` (не хардкод 4:5), `_fit_canvas` добивает остаточный микро-паддинг РАЗМЫТЫМ фоном, не бежевым. Раньше gpt-image давал 2:3 + бежевые полосы. Верифицировано на #7 (печень/хлорофилл 9:16 — кадр заполнен, полос нет, текст чёткий). Автоподпись `_gen_caption` (описание + WB-артикулы, без БАД-дисклеймера) + `regen_caption` + кнопка «В Telegram» (файлом-документом через Aeza-бот, качество не режется).
- ✅ **Сравнение: авто-подбор + формат «симптом→продукт» + авто-детект** (2026-07-22): фича `/compare` расширена. (1) `auto_pick_products` — vision по референсу сам подбирает наши товары под темы/цели (не отметил галочки → берёт «те, что на картинке»). (2) Новый стиль `symptom` — фирменный формат POWERELIX «ЕСЛИ У ТЕБЯ (симптом) → ТОГДА ПЕЙ (товар + артикул WB)»: `_render_symptom` (Pillow: макет/плашки/стрелки/вырез реальной банки/стакан акцентного цвета/весь текст чётко), AI генерит только фото симптомов (`_symptom_illo`: gemini→фолбэк fal-seedream по бежевой подложке, т.к. ProxyAPI-gemini даёт 402), `data/symptom_map.json` — курированные БАД-safe симптомы. (3) `analyze_reference` — vision определяет формат (symptom|lineup), режим «🤖 Авто» по умолчанию. `Comparison.style`. Первый шаг общего движка «узнал формат → собрал под наши товары». (4) Симптом-формат доведён: детальные объёмные иллюстрации (`_ILLO_STYLE` полу-реалистичный рисунок), крупные во всю высоту карточки, авто-ужатие названий (нет наезда банки), ретраи илло (2 gemini + 2 seedream). (5) **Выбор формата картинки** `Comparison.ratio` (4:5/9:16/1:1/3:4) — `_fit_canvas` паддит бежевым без обрезки; селектор на форме и на странице. (6) `restyle(cid, style, ratio)` + переключатель формата/соотношения на детальной странице (чинит устаревший style). (7) Живые логи генерации `_clog`→`<cid>_gen.log` (статик, опрос 3с). ⚠️ Скорость илло зависит от ProxyAPI-Google (gemini ~10-30с; при 402 фолбэк fal-seedream ~5-8 мин на инфографику). TODO: форматы reasons-list/сравнение-колонки/постер.
- ✅ **Выбор движка анимации** (2026-07-17): селект «Движок анимации» на
  раскадровке (колонка video_engine): Seedance 2.0 (дефолт), Seedance fast
  (черновики), Kling 3.0, Grok Imagine, Gemini Omni Flash — все через fal
  (одна касса), `producer.VIDEO_ENGINES`.
- ✅ **Поэтапное производство видео с аппрувом** (2026-07-17): вместо
  «сгенерировать всё разом» — три кнопки на раскадровке: 1️⃣ кадры сцен
  (сетка стиллов, ↻ перегенерация любого кадра) → 2️⃣ анимация Kling
  (клип под каждым кадром, ↻ переанимация сцены; пропускает готовые) →
  3️⃣ сборка (голос Насти + музыка + склейка). Перегенерация кадра
  сбрасывает его клип. `producer.run_stage(stills|clips|assemble)`,
  статусы stills_ready/clips_ready. Кредиты не тратятся на неодобренное.
- ✅ **Ростер моделей + выбор лица** (2026-07-17): 9 лиц бренда
  (assets/brand/models: основная рыжая, 4 девушки, 3 мужчин) — селект
  «Модель в кадре» на раскадровке и карточке поста (колонки model_key),
  producer/generator подставляют выбранное лицо в генерацию.
- ✅ **Видео-стиллы по базе слайдов** (2026-07-17): _produce_video использует
  референсные кадры исходного видео (по таймлайну сцены) + лицо бренда +
  реальную банку — тот же жёсткий промпт, что у Pinterest-ремейков картинок.
- ✅ **Цепочка нейросетей + vision-контроль этикетки** (2026-07-14):
  `producer.gen_product_image` — кадры с продуктом идут по цепочке
  gpt-image-2 (ProxyAPI images/edits, чемпион по кириллице) → gemini flash →
  grok (Replicate); порядок настраивается `CF_IMAGE_CHAIN`
  (дефолт: seedream → nano → gemini → gptimage2 → grok; Seedream 5.0 Pro edit на fal = $0.0675/кадр, самый дешёвый из сильных, этикетку держит). Цены за кадр: nano-banana-2 на
  fal ~$0.08 (≈6₽, USDC из РФ, основной) / Replicate ~$0.07 / тот же gemini на
  ProxyAPI ~20₽ (только фолбэк); gpt-image-2 на fal $0.165 high (основной,
  работает с РФ-VPS) / OpenAI напрямую / ProxyAPI (дороже всех). Скачивание с
  fal.media при РКН — через media-fetcher. gpt-image-2 зовём
  НАПРЯМУЮ в OpenAI (наш ключ, без наценки ProxyAPI; с РФ-VPS — тем же ручным
  multipart через Apify media-fetcher, фолбэк ProxyAPI). После каждой генерации
  Claude-vision сверяет этикетку с
  реальной банкой (product_refs/<id>) и при кривом тексте зовёт следующую
  модель. 402/ошибка модели = мягкий переход дальше. Включено в слайды
  каруселей и стиллы роликов. Правило ремейков: большой заголовок на слайд
  НЕ накладываем — только логотип + дизайн-элементы референса.
- ✅ **Референсы по ссылке: Pinterest** (2026-07-14): поле «Разбор по ссылке»
  на /recon теперь принимает Pinterest-пины (`pinterest.*/pin/…` и короткие
  `pin.it/…`). Загрузчик без API-ключей: публичный widgets-эндпоинт
  (`widgets.pinterest.com/v3/pidgets/pins/info`) → i.pinimg.com с апгрейдом
  размера originals→736x→564x (HEAD-проверка). Пин ложится в Разведку как
  image (topic=pinterest, свой чип), дальше существующий конвейер:
  `deep_analyze_images` (vision-разбор дизайна) → «Сделать похожий» под наш
  товар → storyboard → пост → аппрув → публикация IG+ТГ+ВК. Pinterest
  доступен с РФ-VPS напрямую (проверено). План: `plans/references-pinterest-2026-07-14.md`
  (там же фазы 2-4: библиотека стиль-профилей, доски/аккаунты, видео-пины).

- ✅ **Кросс-пост в Telegram-канал @powerelix** (2026-07-09): после каждой
  успешной публикации в IG контент автоматически уходит и в TG-канал
  («одновременно с Инста», все форматы: фото → sendPhoto+кнопка,
  карусель → sendMediaGroup, Reels → sendVideo+кнопка).
  Архитектура: РФ-VPS не может грузить большие файлы в api.telegram.org
  (CF-релей рвёт большие POST, URL-метод TG капризен + лимит 20МБ), поэтому
  `services/tg_crosspost.py` шлёт **лёгкий JSON** (ссылки на публичные
  /media/) на Aeza-endpoint `POST bot.bandabogachey.online/powerelix-api/crosspost`
  (auth: X-Crosspost-Secret, constant-time) — не-РФ сервер скачивает медиа
  и постит в канал напрямую (`bot/src/api/crosspost.ts` в powerelix-bot).
  Caption: без IG-хэштегов, лимит 1024. Идемпотентность: `posts.tg_message_id`.
  Конфиг: `CF_CROSSPOST=1`, `CF_CROSSPOST_CHANNEL=@powerelix`,
  `CF_CROSSPOST_ENDPOINT`, `CF_CROSSPOST_SECRET` (общий с Aeza `.env`).
  В режиме симуляции IG кросс-пост тоже не шлётся (лог). E2E проверен:
  Reels 26МБ доставлен в личку владельца через полную цепочку.
  ⚠️ Ограничение Bot API: у sendMediaGroup (карусели) кнопок не бывает.
Развиваем этот репо в полноценный сервис на полке Штаба (стек как wb-promotion: FastAPI+SQLite+APScheduler+Jinja2/HTMX). План: `plans/content-factory-service/` (spec/plan/tasks). Конвейер: разведка → план → идеи → генерация → аппрув → публикация → аналитика.
- ✅ **Фаза 1 — фундамент** (2026-06-10): `ig_automation/app.py` (FastAPI + lifespan + scheduler), `db/` (SQLAlchemy 2.0: app_state, trend_reels, hook_analyses, ideas, content_plans, posts, post_assets, post_metrics; create_all), `api/auth.py` (cookie-сессия, dev-режим без пароля), `api/pages.py` (Главная + Статус), `scheduler.py` (heartbeat + refresh токена), шаблоны `web/templates/`. Запуск: `python serve.py` (порт 8010). Проверено вживую: аккаунт @powerelix **BUSINESS** (публикация доступна), токен 57 дн, ProxyAPI поддерживает `messages.parse` (structured output, opus-4-8).
- ✅ **Фаза 2 — Разведка** (2026-06-10): `apify.search_reels` (основной актор `data-slayer/instagram-search-reels` — отдаёт play_count+mp4; фолбэк `instagram-scraper` по хэштегу; `maxTotalChargeUsd`-предохранитель), `services/recon.py` (scrape_topic→trend_reels с дедупом и скачиванием превью; analyze→Claude разбор хука), UI `/recon`. Проверено вживую: тема «похудение» → 12 Reels с реальными просмотрами (36М/27М/24М), разбор хука адаптирует под POWERELIX.
- ✅ **Фаза 3 — Контент-план + Банк идей** (2026-06-10): `services/planner.py` (обёртка `content_plan.generate`→БД + материализация черновиков, идемпотентно), `services/ideas.py` (банк, добавление, идея→черновик), UI `/plan`, `/plan/{id}`, `/ideas`. Проверено: план на 3 поста сгенерирован и материализован.
- ✅ **Материализация по дням** (2026-07-21): `materialize_posts(plan_id, only_date)` — можно отправить в черновики только один день плана (обкатка), неделя остаётся целой. Дедуп по hook (повторный клик не плодит дубли, дни докидываются по одному). `get_plan` отдаёт сводку `days` (date/weekday/done/total) + флаг `_done` на каждом посте. В `/plan/{id}` — полоска кнопок по дням (зелёная = день материализован) + бейдж «✓ черновик». Удаление плана: `/plan/{id}/delete` + `delete_plan` (черновики снять, опубликованные отвязать).
- ✅ **Авто-привязка product_id при материализации** (2026-07-21): `products.resolve_product_id(name)` матчит имя продукта из плана в id линейки (точное по full_name/name → подстрока). `materialize_posts` проставляет `product_id` каждому посту → реальная банка (`producer._product_ref`) и ссылка WB подтягиваются сами, без ручного выбора из пикера. Проверено на всех 11 именах.
- ✅ **Фаза 8 — Деплой** (2026-06-10, поднята раньше срока): сервис на VPS `bidder@195.24.71.21` как systemd `content-factory` (порт 8010, venv py3.10, `.env` через dotenv), nginx-поддомен **`https://content.bandabogachey.online`** (certbot TLS + security-headers, auth-пароль `CF_ADMIN_PASSWORD`), плитка Штаба активирована. Деплой = `git pull && sudo systemctl restart content-factory`. Фикс по ходу: новая сигнатура `Starlette TemplateResponse(request, name, ctx)`. Проверено снаружи (WebFetch): страница входа и плитка живые.
- ✅ **Фаза 4 — Генерация** (2026-06-11): `scenes.generate_branded` (Grok image-edit с референсом лица бренда `assets/brand/ai_model.png` → тот же персонаж, сцена по промпту), `services/generator.py` (generate_post_assets→data/media+PostAsset, generate_post_text→Claude подпись/хэштеги/CTA с БАД-правилами, list_posts/get_post), UI `/posts` + `/post/{id}` (визуал, текст, перегенерация). Pillow в requirements + на VPS. Проверено вживую: визуал с лицом бренда (1.8MB, на тему) + текст БАД-safe. Заметки: 504-фикс — превью IG CDN недоступны с РФ-VPS (fail-fast+выключатель), nginx proxy_read_timeout 300s.
- ✅ **Разведка по темам + AI-фильтр релевантности** (2026-06-11): список фильтруется по теме (чипы, дефолт=последняя тема — раньше глобальный топ забивал узкий запрос). Claude одним батч-запросом при сборе оценивает каждый ролик (relevant + причина): нишевое (здоровье/нутра/витамины/спорт/ЗОЖ) оставляет, развлекательный вирусняк (relatable-юмор, танцы, отношения, фильтр-схемы) прячет. Тумблер «показать всё». Валидация: 15 мусорных роликов (#fyp, «boys vs girls») все помечены 🚫 верно. Колонки `relevant`/`relevance_reason` (миграция).
- ✅ **Разведка по аккаунтам (гибрид)** (2026-06-11): помимо поиска по ключам — сбор топ-Reels конкретного нишевого аккаунта (`apify.account_reels`→`scrape_account`, общий `_store_reels`). Релевантность по построению. Аккаунты идут как `topic=@handle` (чипы). Валидация: ключ «supplements»→0 релевантных (мусор), аккаунт @hubermanlab→6/6 релевантных (2.4М просмотров, чистая ниша). Вывод: для релевантного вирусного контента поиск по ключам слаб, аккаунты — золото.
- ✅ **Бренд-ассеты** (2026-06-11): раздел `/brand` — загрузка лица AI-модели, логотипа и **банок товаров** (привязка к SKU из каталога). `services/brand.py` (add/list/delete, model_ref→дефолт ai_model.png, product_ref→подстрочный матч банки к продукту поста), таблица `brand_assets`. Генерация теперь даёт Grok мультиреференс (лицо + реальная банка) → настоящий продукт с читаемой этикеткой, а не абстрактный POWERELIX. Формат-зависимое соотношение (Reels/Stories 9:16, лента 4:5).
- ✅ **Фаза 5 — Аппрув + БАД-линт** (2026-06-11): `services/compliance.py` (словарь стоп-слов рекламы БАД: лечит/вылечивает/диагноз/гарантирует/избавляет от/от болезни + проверка дисклеймера «не является лекарственным средством» для продуктовых постов), `generator.approve_post` (блок при нарушениях, явный override с пометкой), `add_disclaimer` (вставка стандартного дисклеймера), `back_to_review`. UI на `/post/{id}`: панель с нарушениями (красным), кнопка «Вставить дисклеймер», «Одобрить» / «Одобрить всё равно (override)». Проверено: грязный текст блокируется, чистый одобряется.
- ✅ **Фаза 6 — Публикация** (2026-06-11): `instagram.py` (create_image_container → container_status-поллинг → publish_container → permalink), `services/publisher.py` (идемпотентная `publish`, `schedule`, `publish_due`; картинка отдаётся IG по публичному `CF_PUBLIC_BASE`/media/...), `scheduler.py` джоб `publish_due` каждую минуту. UI `/post/{id}`: «Опубликовать сейчас» + «Запланировать (МСК)». **Режим симуляции** (`CF_SIMULATE_PUBLISH=1`) — в IG ничего не уходит, пишет «как бы опубликовано»; боевой постинг = флаг `0` + restart. Проверено в симуляции: publish→published/SIMULATED, идемпотентность, планировщик.
- ✅ **Фаза 7 — Аналитика** (2026-06-11): `instagram.get_media_insights` (reach/saved/likes/comments/shares), `services/insights.py` (pull/pull_all/overview), джоб `pull_insights` каждые 6ч, UI `/analytics` (сводка: всего/реально/симуляция, средний охват по рубрикам, посты с метриками). Insights только для реальных постов (симуляция метрик не имеет).

- ✅ **Карусели + логотип** (2026-06-11): `generator.generate_carousel` (N слайдов: герой + контент-кадры с лицом/продуктом бренда, доп-промпты на слайд), лого-оверлей `_apply_logo` (правый нижний угол, если загружен в /brand), `brand.logo_ref`. UI: кнопка «Сген. карусель» для постов-каруселей. ⚠️ генерация Grok требует пополнения xAI-баланса (на 2026-06-11 кредиты xAI кончились — 403 permission-denied).

- ✅ **Движок Б — UGC-CRM блогеров, B1** (2026-06-11): таблицы `bloggers` (ник/платформа/ниша/подписчики/контакт/статус lead-active-ambassador-blacklist) + `deals` (воронка 11 стадий, collab_type, промокод, подменный артикул WB, UTM, **erid маркировка**, атрибуция orders/revenue, площадка). `services/bloggers.py`, UI `/bloggers` (список+добавление), `/blogger/{id}` (карточка + сделки: стадия, поля атрибуции/комплаенса, исход won/lost), `/pipeline` (доска по стадиям). Нав-ссылка «📣 Блогеры».
- ✅ **B2 — система коммуникации** (2026-06-11): шаблоны сообщений (`message_templates`, плейсхолдеры {name}/{handle}/{niche}/{product}, UI `/templates`) — на карточке блогера рендерятся под него, копируешь и шлёшь руками (IG не даёт авто-директ). Follow-up: `deals.last_touch_at/next_followup_at` (миграция), «записать касание» (+напоминание через N дн), список «⏰ требуют касания» на `/bloggers`. Комплаенс: предупреждение «🚫 Instagram запрещён в РФ» на сделке + напоминание про erid на стадиях контент/аппрув/опубликовано.
- ✅ **B3 — deliverables + права + ROI** (2026-06-11): `deliverables` (что блогер выложит: формат/площадка/срок/статус requested→published, на карточке сделки), права на UGC (`deals.rights_repost/rights_ads/rights_term` — миграция, чекбоксы в форме сделки), импорт выручки `/wb-import` (вставляешь «промокод заказы выручка» → атрибуция по совпадению промокода в сделки) + сводка (блогеры/сделки/заказы/выручка). **Блогерская CRM (движок Б) полностью готова: B1 база+воронка, B2 коммуникация, B3 deliverables+права+ROI.**

- ✅ **Пост → конкретный товар → текст с артикулом** (2026-06-12): на посте пикер товара из каталога (11 SKU из brand_powerelix.json), привязка `posts.product_id`; страница `/catalog` — внести артикул WB (nmId) + ссылку + акцент на каждый товар (`product_links`); `generate_post_text` адаптирует текст под конкретный товар (полный контекст: польза/вещества/слоган) и **гарантированно вставляет артикул+ссылку WB в подпись**; визуал тоже идёт под привязанный товар (банка по каноничному названию). `services/catalog.py`, `products.one_context/products_list/product_by_id`.

- ✅ **Гибкость генерации** (2026-06-12): референсы под пост (`PostAsset` kind="ref" — загрузка своей банки/сцены, отделены от слайдов; генерация добавляет их в refs к лицу+товару), выбор слайдов карусели (3-7), удаление слайдов/референсов. `generator.add_post_ref/delete_post_asset`; publisher и get_post фильтруют kind="image".

- ✅ **Reels: сценарий + раскадровка + видео** (2026-06-12): `generate_reels_script` (Claude → хук, 3-5 сцен с раскадровкой visual/onscreen/voiceover, CTA с WB-артикулом, длина, звук; `posts.reels_script` JSON), `generate_reels_video` (hero-кадр 9:16 с лицом бренда → image→video через Replicate `scenes.generate_video`/`_call_replicate_video` grok-imagine-video → PostAsset kind=video). UI секция Reels на посте. Проверено: сценарий 5 сцен, видео 1.9МБ за 49с. ⚠️ видео — короткий клип по hero-кадру; полная сборка многосценного Reels с озвучкой — будущий шаг.

- ✅ **Батч UX+Reels (2026-06-12):** (1) Дашборд **«Что сегодня»** на главной — посты к публикации/проверке/доработке + блогеры на фоллоу-ап (`pages._today`), вместо устаревшего «Статус сборки». (2) **Поиск по CRM** `/search` (блогеры/посты/сделки, Python-side для корректной кириллицы) + строка на главной и в навигации. (3) **Telegram-напоминалки** — `services/notify.py` (через CF-релей tg-relay, no-op без `CF_TG_TOKEN`/`CF_TG_CHAT`), ежедневная сводка задач 09:00 МСК (`scheduler._followup_reminders`). (4) **Многосценный Reels с озвучкой** — `services/reels.py`: на каждую сцену reels_script картинка с лицом бренда → клип image2video, русская озвучка **по сценам** (Replicate **minimax/speech-02-turbo**, т.к. OpenAI-TTS заблокирован с РФ-VPS) → каждая сцена держится в кадре ровно под свою озвучку (**смена сцен в такт голосу**, `_assemble_synced`: video concat-copy + audio concat-filter + мукс), 1080×1920, через **ffmpeg**; запуск в фоновом потоке (`start_full_reels`, ~6-8 мин). **Устойчивость:** видео-генерация ретраится x3, упавшая сцена пропускается (одна флак-сцена не убивает ролик). Озвучка сцен короткая (промпт сценария: 1 фраза ≤12 слов, ролик 20-35 сек), TTS speed 1.08. Проверено на проде сквозняком (разные сцены меняются, банка с читаемой этикеткой, со звуком). Config: `CF_TTS_MODEL/CF_TTS_VOICE`. **numpy доставлен на VPS** (для brand_overlay).

- ✅ **Чистая сцена + фирменная обложка-текст** (2026-06-12): AI коверкает текст в кадре (особенно кириллицу). (а) Сцену извлекает Claude (`_scene_description`) — из сценария-идеи делает описание ОДНОГО фото-кадра без текста/реплик/бренд-нейма; референс-банка привязана жёстко (with_product_ref при любом референсе); из стиля убрано слово POWERELIX (модель писала его буквами). Картинка генерится ЧИСТОЙ. (б) Текст накладываем сами — **движок `brand_overlay.py`** (тот же, что эталонные карусели `build_post01`): стиль ОБЛОЖКИ = вордмарк POWERELIX (Montserrat Black, БЕЗ логотипа) + заголовок Montserrat Black капсом + акцент-черта `#00C29B` + подзаголовок (Inter SB) + тег «СОХРАНИ →» + дисклеймер БАД; раскладка якорится снизу вверх с зазорами (не слипается). `overlay.render_cover`; `generator.suggest_overlay_text` (Claude → заголовок+подзаголовок+тег+дисклеймер) + `apply_text_overlay`. Шрифты Inter/Montserrat в `assets/fonts/` (brand_overlay переведён с Mac-шрифтов на bundled). UI: «✨ Авто-текст» + форма (заголовок/подзаголовок/тег/дисклеймер). **+numpy на VPS** (нужен `_scrim`). Стиль ЗАФИКСИРОВАН пользователем 2026-06-12, проверено на проде. ⚠️ был тупик: HTML/Playwright + свой кривой overlay с белыми плашками — выкинуто, эталон уже был в репо (`brand_overlay`).

- ✅ **Разделение блогеров + история** (2026-06-12): пост можно привязать к блогеру (`posts.blogger_id` — пикер «📣 Для кого: свой аккаунт / блогер X» на посте); на карточке блогера — секция «📝 Контент для блогера» (привязанные посты со статусом, кликабельны). Контент-завод (свой аккаунт) и блогерская CRM теперь связаны: контент для блогера находится в его истории.

**🎉 КОНВЕЙЕР MVP ГОТОВ (Фазы 1-7):** разведка (темы+аккаунты+AI-фильтр+видео) → план → банк идей → генерация (лицо бренда+банки) → аппрув+БАД-линт → публикация (Graph API+планировщик, режим симуляции) → аналитика. Всё задеплоено на `content.bandabogachey.online`. Боевой постинг включается флагом `CF_SIMULATE_PUBLISH=0`.
- Примечание: IG-приложение в **dev-режиме** с `content_publish` (аккаунт — админ) → возможно, реальный постинг заработает **без App Review**; флаг `CF_SIMULATE_PUBLISH=1` оставлен как страховка до проверки в Фазе 6.

## ✅ Готово
- Подключён официальный Instagram API (Instagram Login): приложение Meta «POWERELIX Automation», аккаунт @powerelix (BUSINESS), долгоживущий токен 60 дней.
- Все 5 разрешений в dev-режиме: basic, content_publish, manage_comments, manage_messages, manage_insights.
- Каркас проекта: config/.env, обёртка Instagram API (профиль, медиа, refresh токена), линейка продуктов.
- **Генератор контент-плана** через Claude (structured output, юр. правила БАД, баланс рубрик/форматов) — `generate_plan.py`.
- Проверка связи — `check_connection.py`.

## 🔜 В работе / следующее
- [ ] Вставить `ANTHROPIC_API_KEY` в `.env` и прогнать первый контент-план.
- [ ] Фаза «Публикация»: загрузка медиа + публикация поста/Reels/Stories через Content Publishing API (очередь + расписание).
- [ ] Фаза «Генерация медиа»: связка с uniqualizer (фото/видео) + промпты из prompt-lab по плану.
- [ ] Автопродление токена (cron) + запись нового токена в .env.

## 🚀 SYNTX-техники + UGC-блогеры (роадмап, из разбора @syntxfamily 2026-07-22)
Каталог 385 техник → `prompt-lab/syntx-techniques.md` + `sources/syntx/`. Внедряем под POWERELIX.

**🎤 UGC-блогеры (говорящие аватары) — приоритет, ядро готово:**
- ✅ **Ядро** (2026-07-22): `services/avatar.py` — ChatGPT UGC-сценарий (BAD-safe) → старт-кадр с РЕАЛЬНОЙ банкой (nano) → **Veo 3 fast i2v оживляет+озвучивает по-русски** (приём SYNTX id 2158). Режимы i2v (банка наша, реком.) / t2v (быстрее, банка генерная). Персоны nutri/mom/fit. Проверено: банка сохраняется, вайб UGC живой.
- ✅ **Лицо блогера + рабочий липсинк** (2026-07-22): по кадрам блогер выглядел «нездоровым» — промпт старт-кадра сам просил «минимум макияжа / amateur / без ретуши» + не давал лицо-референс → nano лепил уставшую тётку с мешками/краснотой. Фикс: PERSONAS + `_start_frame` → «здоровая сияющая кожа, выспавшийся вид, ясные глаза, без тёмных кругов/отёка/красноты». Липсинк «губы не попадали» = **вообще не запускался**: `reels._public_url` терял подпапку (`/media/bloggers/x` → `/media/x` = 404) → липсинк молча падал, звук миксовался поверх. Фикс URL (путь относительно MEDIA_DIR). Движки: `fal-ai/sync-lipsync/v2` (Sync-2, queue API) реализован, но **fal.media режется РКН с РФ-VPS в обе стороны** → за флагом `CF_LIPSYNC_SYNC2` до CF-хостинга медиа; дефолт — **latentsync (Replicate)**, единственный иностранный сервис, что достаёт наш домен. Проверено end-to-end: лицо здоровое, рот артикулирует под речь Nastya.
- [ ] **Докрутить озвучку** — есть неточности в речи (Veo); тестить промпт речи, темп, паузы; при плохом — фолбэк на наш reels-пайплайн (i2v без звука + MiniMax TTS + Replicate-липсинк).
- [ ] **UI `/blogger`**: выбрал товар+персону+акцент → воркер генерит → превью видео.
- [ ] **Ростер персон** — консистентные лица блогеров (face-ref, как у бренд-модели), несколько типажей.
- [ ] Оверлей вордмарка/сабтитры + сборка в финальный Reels + публикация.

**Внедрить из шорт-листа SYNTX (по каждому — точный промпт в `sources/syntx/catalog-N.md`):**
- [ ] **Разбор состава парящими слоями** (id 3588) — образовательный Reels: банка→витамины с подписями/весом.
- [ ] **«Preserve 100%» + hex-фон** (id 4886) — усилить продукт-шоты (фон под лайм→мята, жёсткий лок этикетки).
- [ ] **ChatGPT-конвейер** в `/plan`: продающие смыслы за 7 запросов (id 3344), реверс текстов конкурентов WB (id 2910), контент-планы AIDA → в `content_bank.md`.
- [ ] **N ракурсов/арт-сцен из 1 фото банки** (id 2529/2926/3868/3641) — для карточек WB и постов.
- [ ] Quick-wins: эталонный негатив против искажений лиц (id 1343), настройки камеры `Kodak Portra 400/f2.8/visible pores` (id 5107), свет одним тегом (id 3970).
- [ ] LoRA на банку (id 1252) — оценить ROI для 100% узнаваемости.

## 📋 Бэклог
- [ ] Аналитика (Insights) — когда у аккаунта появятся посты и 100+ подписчиков.
- [ ] Ответы на комментарии/Direct (manage_comments / manage_messages).
- [ ] Мониторинг конкурентов (read-only).
- [ ] Веб-кабинет/превью (возможно, перенос в wb-promotion или отдельный UI).
- [ ] App Review для Live-режима (если понадобятся чужие аккаунты).

## Заметки
- Контент-план соблюдает РФ-правила рекламы БАД: «не лекарство», мягкие формулировки, дисклеймер.
- Аккаунт новый (0 постов, 1 подписчик на 2026-06-08) → приоритет на наполнение, не на аналитику.
- **Контент-стратегия (из разбора @chatplaceio, 2026-06-09):** распределять посты **Viral (охват) / Expert (доверие) / Sales (продажи)** — та же логика, что 70/20/10. **Карусели — приоритетный формат 2026** (под нашу ставку). Хуки/заголовки и формула вирусного поста — в `prompt-lab/playbook-headlines.md`. Репёрпосинг: один смысл → Reels + карусель.

## 🎯 Сравнение: режим «точная копия» (2026-07-23)
- ✅ **Фаза 1-2 (ядро+UI):** новый `style=aicopy` — gpt-image-2 воссоздаёт МАКЕТ
  референса 1:1 (любой, вкл. премиум node/glow), подставляет наши банки,
  перекрашивает акценты в бренд-зелень (лайм #C3FF08 → мята #16FFB3), убирает
  чужой бренд. `comparison._execute_aicopy`, диспатч в `execute`, радио
  «🎯 Точная копия референса» в `compare.html`/`compare_detail.html`. Проверено
  на простом (магний) и богатом (node «добавки для мужского здоровья») референсах +
  end-to-end через пайплайн (cid 5, done). Старые режимы lineup/symptom не тронуты.
- ⏳ **Фаза 3 (оверлей):** чистый слой поверх — артикулы WB под банками (гарант. читаемость мелкого текста).
- ⏳ **Фаза 4 (маппинг):** умное соответствие товар↔слот (N слотов референса → N наших товаров).

## 📡 UGC-контур: источники → идеи → инфографика (2026-07-23)
Отдельный контур для UGC-блогеров (по playbook qeep: виральные инфографики → трафик на WB).
- ✅ **Кирпич 1-2 — Источники**: `SourceAccount` + `/sources` — добавляешь IG-аккаунты
  (конкурент/донор), «Разведать» тянет посты через `recon.scrape_account` → TrendReel.
- ✅ **Кирпич 3 — Банк идей**: `sources.generate_ideas(source_id|None)` — Claude генерит
  оригинальные концепты инфографик под наши товары (из постов источника или с нуля,
  виральные формулы + козыри бренда) → таблица `ideas`. Кнопки «💡 Идеи».
- ✅ **Кирпич 4 — В картинку**: `sources.generate_from_idea` (фон-поток) — концепт → gpt-image
  → брендовая инфографика (`ideas.image_path`). Кнопка «🎨 Инфографика» на карточке идеи.
  Проверено: из чистого концепта («5 причин просыпаться разбитой») → готовая инфографика
  симптом→продукт с нашими товарами + «274000+ выбрали нас».
- ⏳ **Позже — Мультиюзер**: логины блогерам, роли, изоляция данных (отложено до готового каркаса).
